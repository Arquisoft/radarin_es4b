import React from 'react';
import {Text} from 'react-native';

const Header = () => (
  <Text
    style={{
      backgroundColor: '#282c34',
      color: 'white',
      textAlign: 'center',
      fontSize: 30,
      padding:5
    }}>
    Radarin
  </Text>
);

export default Header;
